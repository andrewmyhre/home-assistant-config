raise_lights:
  sequence:
  - service: light.turn_on
    data_template:
      entity_id: light.main_lights
      brightness: 30
      color_name: "Warm White"
  - service: light.turn_on
    data_template:
      entity_id: light.main_lights
      brightness: 60
      color_name: "Warm White"
  - service: light.turn_on
    data_template:
      entity_id: light.main_lights
      brightness: 90
      color_name: "Warm White"
  - service: light.turn_on
    data_template:
      entity_id: light.main_lights
      brightness: 120
      color_name: "Warm White"
